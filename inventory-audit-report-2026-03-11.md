# 库存系统加减逻辑审计报告

- 项目：`tapePrinter`
- 审计日期：2026-03-11
- 审计范围：库存加减相关功能（入库、出库、库存调整、打印扣减、配货出库/重置、Excel 导入、库存日志撤销）
- 审计方式：代码静态审查（后端 + 前端 + 权限链路）

---

## 一、审计结论

系统核心流程可运行，但存在会导致库存错误或越权操作的关键问题。  
其中 3 项为高风险（P0），建议优先修复后再继续扩展功能。

---

## 二、问题清单（按严重级别）

## P0（高风险）

### 1) 配货“不拆”出库单位换算错误，存在 1000 倍扣减风险

- 现象：
  - 配货页面“不拆”弹窗输入单位是“只”。
  - 后端库存主单位在出入库页面和日志中使用“千”。
  - 不拆分支扣库存时未做 `/1000` 换算，直接按“只”扣减。
- 风险：库存会被超额扣减（量级错误），导致库存数据严重失真。
- 位置：
  - `src/renderer/src/pages/PickingOrderPage.tsx`（不拆弹窗输入“只”）
  - `src/main/database.ts`（`confirmPickingItems` 普通出库分支直接 `subInventory.run(thisOutQty, ...)`）

### 2) 后端库存增减接口缺少强校验，可被构造请求反向改库存

- 现象：
  - `stockIn`/`stockOut`/`setInventory`/`print` 路由层未做严格数值校验。
  - 数据层 `stockIn`/`stockOut` 也未强制 `quantity > 0`。
  - 任何已登录用户可绕过前端限制直接传负数。
- 风险：
  - 入库传负数可变相出库；
  - 出库传负数可变相入库；
  - 造成库存篡改面。
- 位置：
  - `src/main/api-routes.ts`
  - `src/main/database.ts`

### 3) 配货出库与重置接口仅鉴权未鉴权能，存在越权库存变更

- 现象：
  - `/picking-orders/confirm-pick`、`/picking-orders/reset-pick` 仅使用 `authMiddleware`。
  - 未要求库存相关权限或配货管理权限。
- 风险：普通登录用户可直接触发库存扣减/恢复。
- 位置：
  - `src/main/api-routes.ts`

---

## P1（中风险）

### 4) “导入库存（覆盖）”日志与实际库存变化不一致

- 现象：
  - `importInventory` 是“覆盖库存到目标值”。
  - 日志写入 `in/out + abs(qty)`，使用的是目标值，而不是“差值”。
- 风险：审计日志无法准确反映真实变化过程，影响追溯与对账。
- 位置：
  - `src/main/database.ts`（`importInventory`）

### 5) 配货时物料不存在库存系统，仍可标记已出库

- 现象：
  - 物料不存在时仅记录 `inventoryErrors`，但仍更新配货状态（含已出库）。
- 风险：出现“配货完成但库存未扣减”的业务断层。
- 位置：
  - `src/main/database.ts`（`confirmPickingItems`）

---

## P2（低风险 / 设计缺陷）

### 6) 库存日志支持物理删除，破坏完整追溯链

- 现象：管理员可批量删除库存日志，且不自动反算库存。
- 风险：库存值与操作历史可脱节，不利于审计。
- 位置：
  - `src/main/database.ts`（`deleteInventoryLogs`）
  - `src/main/api-routes.ts`（`/inventory/logs/batch-delete`）

---

## 三、修复优先级建议

1. **先修 P0**  
   - 统一库存单位规则（明确库存表主单位），配货“不拆”严格换算。  
   - 在路由层和数据层双重做参数校验（`Number.isFinite`、边界、`>0`）。  
   - 给配货确认/重置接口加权限中间件。

2. **再修 P1**  
   - `importInventory` 按“差值”记日志。  
   - 对“不存在库存物料”的配货策略做强约束（阻断或进入待处理状态）。

3. **最后处理 P2**  
   - 建议改为“不可物理删日志”，仅支持“撤销”或“软删除 + 审计留痕”。

---

## 四、建议回归验证清单

- 入库/出库/打印接口对非法参数（负数、NaN、极值）全部拒绝。
- 配货“不拆”输入“只”时，库存扣减按“千”正确换算。
- 无权限用户无法调用配货确认与重置接口。
- 导入库存后，日志与库存差值一致。
- 撤销、重置、批量操作后，库存值与日志可相互解释。

---

## 五、备注

本报告为静态代码审计结论，未包含真实运行环境的并发压测与历史数据抽样核对。  
建议在修复后执行一轮联调验证与关键用例回归。

