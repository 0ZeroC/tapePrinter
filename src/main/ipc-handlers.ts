// IPC handlers are no longer needed - all data operations go through the Express HTTP API.
// This file is kept for compatibility but can be removed.
export function registerIpcHandlers(): void {
  // No-op
}
